const config = {
  // For local development - uncomment this line and comment the production URL when working locally
  // apiUrl: 'http://localhost:5000/api',
  
  // For production - using Render backend URL
  apiUrl: 'https://date2mate.onrender.com/api'
};

export default config;
